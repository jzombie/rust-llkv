statement ok
CREATE TABLE maybe (id INT NOT NULL, note TEXT);

statement ok
INSERT INTO maybe (id, note) VALUES (1, 'present');

statement ok
INSERT INTO maybe (id, note) VALUES (2, NULL);

query T rowsort
SELECT note FROM maybe;
----
present

